#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(){
    int n;
    cin>>n;

    cout<<n + (n / 2) * 2 + (n / 3) * 2 <<endl;
}
int main(){
    int t; cin>>t;
    
    while(t--){
        solve();
    }
    return 0;
}