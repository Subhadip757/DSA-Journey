#include<bits/stdc++.h>
using namespace std;

vector<int> dijkstra(int n, vector<vector<int>> &graph, int src, int dst){
    vector<int> adj[n];
    
    for(int i = 0; i < n; i++){
        for(auto edge : graph[i]){
            adj[i] = edge[]
        }
    }
}

int main(){

}